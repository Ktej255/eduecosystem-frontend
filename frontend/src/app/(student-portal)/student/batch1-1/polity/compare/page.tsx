import CompareMaster from "@/components/batch1-1/polity/CompareMaster";

export const metadata = {
    title: "Compare Master | Indian Polity | UPSC Prep",
    description: "UPSC polity comparison tables — President vs Governor, LS vs RS, FRs vs DPSP and more.",
};

export default function ComparePage() {
    return <CompareMaster />;
}
