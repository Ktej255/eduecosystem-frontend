"use client"

import { ToastProvider } from "@/components/ui/Toast"

// Toaster component that wraps the custom toast system
// The ToastProvider is already included in the app layout
export function Toaster() {
    // Return null as toast rendering is handled by ToastProvider  
    return null;
}
